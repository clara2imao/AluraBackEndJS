// tipagem dinâmica -> característica do JS - quando não precisamos declarar o tipo
let minhaVar = 567;
minhaVar="texto"; 
minhaVar = true;// o próprio JS reconhece o tipo da variável

//Multiparadigma -> Consegue resolver o mesmo problema de várias formas diferentes

//Oque é o ES6/ES2015 -> Versão do java que saiu em 2015 e é a versão mais famosa (ex: let e const)

//Linguagen interpretada->  escreve o código e o código vai ser executado na hora, já na compilada ele passa a linguagem por um compilador que passa para "linguagem de computador"

//O que é o NodeJS-> o NodeJS assim como o navegador ele é o interpretador da linguagem

